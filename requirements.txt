# Stages 1-8 — Drive auth, ingestion, features, strategies, dashboard.
python-dotenv==1.0.1
google-auth==2.32.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.140.0
#pandas==2.2.2
requests==2.32.3
# yfinance is NOT needed here — app.py reads from Drive parquets only.
# It is installed separately in daily.yml (Phase 1 pipeline) and scripts/requirements.txt (Phase 2).
# curl-cffi is pulled in by yfinance and 0.15.x causes malloc corruption on Streamlit Cloud —
# keeping yfinance out of this file keeps Streamlit Cloud healthy.
#pyarrow==17.0.0
streamlit==1.38.0
plotly==5.24.0
xlrd==2.0.1
beautifulsoup4==4.12.3
lxml==5.3.0
# IMPORTANT: keep pandas < 3.0. pandas 3.0 wheels are built against the numpy 2.x
# ABI; with numpy<2.0.0 below that mismatch SEGFAULTs Streamlit Cloud on import.
numpy<2.0.0
pandas>=2.2.3,<3.0
pyarrow>=18.0.0,<19.0.0
